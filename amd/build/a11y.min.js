/**
 * Accessible names from the language pack for the editor's own controls.
 *
 * MathQuill 0.10.1 renders a hidden textarea without a label; screen readers then announce an
 * unnamed edit field. The row buttons of the multi-line editors are icons. Both get their name
 * from the language pack here, so the name follows the user's language.
 *
 * @module     local_stackmatheditor/a11y
 * @copyright  2026 Ralf Erlebach
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_stackmatheditor/a11y",["core/str"],(function(Str){var preloaded={};function label(el,key,withTitle){if(el)return preloaded[key]?(el.setAttribute("aria-label",preloaded[key]),void(withTitle&&el.setAttribute("title",preloaded[key]))):void Str.get_string(key,"local_stackmatheditor").then((function(text){return el.setAttribute("aria-label",text),withTitle&&el.setAttribute("title",text),text})).catch((function(){}))}return{useStrings:function(strings){Object.keys(strings||{}).forEach((function(key){preloaded[key]=strings[key]}))},label:label,labelEditor:function(field){label(field.el().querySelector("textarea"),"aria_formula_input")}}}));

//# sourceMappingURL=a11y.min.js.map