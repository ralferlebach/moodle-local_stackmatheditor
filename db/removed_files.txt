# Files removed in earlier releases of this plugin.
#
# Updating a repository or an installation in place (e.g. by unpacking a patch ZIP) leaves a
# removed file behind, where it keeps being autoloaded and executed. Moodle's own ZIP installation
# replaces the plugin directory as a whole and is not affected, which is why this file is excluded
# from the release archive.
#
# The stale-files CI job fails while any of these paths still exists. Remove them with git rm.

# 2026091000: replaced by moodle-plugin-ci-dev.yml / moodle-plugin-ci-main.yml. Left in place it
# would run a second, outdated pipeline on every push.
.github/workflows/moodle-ci.yml
# 2026091000: IDE project files were committed despite .gitignore (git rm -r --cached .idea).
.idea
