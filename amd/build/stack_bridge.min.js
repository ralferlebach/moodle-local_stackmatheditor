/**
 * Bridge between the visible MathQuill editors and the original STACK inputs (#48).
 *
 * Guiding invariant: the expression visible in MathQuill is the expression STACK receives.
 *
 * - triggerValidation(): tells STACK (and any other script) that the original input changed.
 *   Exactly one native "input" and one native "change" event. Native events reach jQuery
 *   handlers as well, so the former extra jQuery triggers only produced duplicates.
 * - register(flush): every editor registers a function that writes its current state into the
 *   original input without raising events. All of them run in a capture-phase listener before
 *   a form is submitted (and already on pointerdown of a submit button), so "Check" and
 *   "Submit" never send a value that a pending debounced sync has not written yet. The native
 *   event path is not touched: no preventDefault, no own submit logic.
 * - guardStaleValidation(): STACK shows an "invalid" AJAX response without checking whether
 *   it still belongs to the current value. A slow response for a transient state such as
 *   "(2)/()" can therefore arrive after the final "(1)/(6)" was validated and overwrite it.
 *   When that can have happened, validation of the current value is requested once more;
 *   STACK answers from its cache if the value was already validated.
 *
 * Integration contract for external scripts (#43) - see README.md, "Integration events".
 * All events bubble and carry detail.source = 'local_stackmatheditor':
 * - stackmatheditor:input        on the original input, after the editor changed its value;
 * - stackmatheditor:enter        on the original input, for Enter in the visible editor and
 *                                for the "+" (add row) buttons; plus a non-bubbling keydown /
 *                                keyup Enter mirror on the original input;
 * - stackmatheditor:beforecheck  on a STACK Check button, after all editors were flushed;
 * - stackmatheditor:beforesubmit on the form, after all editors were flushed.
 * The editor never cancels, replaces or isolates the native events.
 *
 * @module     local_stackmatheditor/stack_bridge
 * @copyright  2026 Ralf Erlebach
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_stackmatheditor/stack_bridge",[],(function(){var flushers=[],installed=!1,state="function"==typeof WeakMap?new WeakMap:null;function stateOf(el){var s;return state?((s=state.get(el))||(s={dispatched:0,revalidated:null},state.set(el,s)),s):(el.smeBridgeState=el.smeBridgeState||{dispatched:0,revalidated:null},el.smeBridgeState)}function emit(target,name,detail){var data={source:"local_stackmatheditor"};Object.keys(detail||{}).forEach((function(key){data[key]=detail[key]})),target.dispatchEvent(new CustomEvent("stackmatheditor:"+name,{bubbles:!0,detail:data}))}function flushAll(){flushers.forEach((function(fn){try{fn()}catch(e){}}))}function submitControl(target){var type,el=target&&target.closest?target.closest("button, input"):null;return el&&el.form&&("submit"===(type=(el.getAttribute("type")||("BUTTON"===el.tagName?"submit":"")).toLowerCase())||"image"===type)?el:null}return{STACK_TYPING_DELAY:1e3,EVENT_PREFIX:"stackmatheditor:",triggerValidation:function(el){stateOf(el).dispatched=Date.now(),el.dispatchEvent(new Event("input",{bubbles:!0,cancelable:!0})),el.dispatchEvent(new Event("change",{bubbles:!0,cancelable:!0})),emit(el,"input",{name:el.name,value:el.value})},signalEnter:function(el,info){var detail={name:el.name,trigger:"key"};Object.keys(info||{}).forEach((function(key){detail[key]=info[key]})),emit(el,"enter",detail),["keydown","keyup"].forEach((function(type){var ev=new KeyboardEvent(type,{key:"Enter",code:"Enter",bubbles:!1,cancelable:!0});Object.defineProperty(ev,"keyCode",{value:13}),Object.defineProperty(ev,"which",{value:13}),el.dispatchEvent(ev)}))},register:function(fn){installed||"undefined"==typeof document||(installed=!0,document.addEventListener("pointerdown",(function(e){submitControl(e.target)&&flushAll()}),!0),document.addEventListener("keydown",(function(e){"Enter"!==e.key&&" "!==e.key||!submitControl(e.target)||flushAll()}),!0),document.addEventListener("click",(function(e){var control=submitControl(e.target);control&&/-submit$/.test(control.getAttribute("name")||"")&&(flushAll(),emit(control,"beforecheck",{name:control.getAttribute("name")}))}),!0),document.addEventListener("submit",(function(e){flushAll(),emit(e.target,"beforesubmit",{submitter:e.submitter?e.submitter.getAttribute("name"):null})}),!0)),flushers.push(fn)},flushAll:flushAll,guardStaleValidation:function(el){el.addEventListener("stack-validation",(function(e){var value,detail=e.detail||{},s=stateOf(el);detail.completed&&!1===detail.valid&&(Date.now()-s.dispatched<1e3||(value=function(el){return String(el.value||"").replace(/^\s+|\s+$/g,"")}(el),s.revalidated!==value&&(s.revalidated=value,el.dispatchEvent(new Event("input",{bubbles:!0,cancelable:!0})))))}))}}}));

//# sourceMappingURL=stack_bridge.min.js.map