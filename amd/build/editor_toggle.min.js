/**
 * On/off switch for the formula editor (#13).
 *
 * A student can put the editor away - to see more of the question on a phone, or to type STACK
 * syntax directly. Switching off hands the current answer to the original STACK input in Maxima
 * syntax and puts that input back where it belongs; switching on takes whatever is in the input
 * now, including anything typed while the editor was away, back into the editor.
 *
 * The module owns the switch and the two CSS classes, nothing else: what "hand over" means is
 * the caller's business, because only the caller knows whether it is driving one field or a
 * system of equations.
 *
 * Deliberately free of dependencies: plain DOM, so the behaviour can be tested without a
 * browser and without MathQuill.
 *
 * @module     local_stackmatheditor/editor_toggle
 * @copyright  2026 Ralf Erlebach
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_stackmatheditor/editor_toggle",[],(function(){var STORAGE_KEY="local_stackmatheditor_editor_off",switchCount=0;function remember(off){try{window.localStorage.setItem(STORAGE_KEY,off?"1":"0")}catch(e){return}}function hideOriginal(input){input.classList.add("sme-original-hidden")}function showOriginal(input){input.classList.remove("sme-original-hidden")}return{STORAGE_KEY:STORAGE_KEY,HIDDEN_CLASS:"sme-hidden",HIDDEN_INPUT_CLASS:"sme-original-hidden",startsOff:function(){try{return"1"===window.localStorage.getItem(STORAGE_KEY)}catch(e){return!1}},remember:remember,hideOriginal:hideOriginal,showOriginal:showOriginal,create:function(spec){var strings=spec.strings||{},id="sme-editor-switch-"+ ++switchCount,element=document.createElement("div"),box=document.createElement("input"),label=document.createElement("label"),status=document.createElement("span");function apply(on,initial){on?(initial||spec.toEditor(),hideOriginal(spec.input)):(spec.toInput(),showOriginal(spec.input)),(spec.editor||[]).forEach((function(part){part&&(on?part.classList.remove("sme-hidden"):part.classList.add("sme-hidden"))})),box.checked=on,status.textContent=on?strings.toggle_editor_on||"Formula editor on.":strings.toggle_editor_off||"Formula editor off.",element.setAttribute("title",status.textContent),initial||remember(!on)}return element.className="sme-toggle form-check form-switch",box.type="checkbox",box.id=id,box.className="form-check-input sme-toggle-input",box.setAttribute("role","switch"),box.setAttribute("aria-label",strings.toggle_editor||"Formula editor"),label.setAttribute("for",id),label.className="form-check-label sme-toggle-label",label.textContent=strings.toggle_editor||"Formula editor",status.className="sme-toggle-status sr-only visually-hidden",status.setAttribute("aria-live","polite"),element.appendChild(box),element.appendChild(label),element.appendChild(status),box.addEventListener("change",(function(){apply(box.checked,!1)})),{element:element,apply:apply,input:box}}}}));

//# sourceMappingURL=editor_toggle.min.js.map